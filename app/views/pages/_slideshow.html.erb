<!-- Indicators -->
  <%= render @photos %>

  <%= content_for :script do %>
  <script type="text/javascript">
  var lastPhoto = <%= @photos.first.created_at.to_i %>
  var photoCount = <%= Event.current.photos.count %>
  </script>
  <% end %>