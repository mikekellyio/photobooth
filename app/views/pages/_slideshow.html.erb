<!-- Indicators -->
  <%= render @photos %>

  <%= content_for :script do %>
  <script type="text/javascript">
  var lastPhoto = new Date("<%= Event.current.sorted_photos.first.created_at %>")
  var photoCount = <%= Event.current.photos.count %>
  </script>
  <% end %>