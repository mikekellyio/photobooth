if(lastPhoto < <%= Event.current.sorted_photos.first.created_at.to_i %>  ){
  lastPhoto = <%= @photos.present? ? @photos.first.created_at.to_i : 0%>

  $('#photos').prepend('<%=j render @photos %>')

  $('#photos img').each(function(){
    $(this).load(function(){
      $(this).addClass('loaded')
    })
  })
}
console.log(photoCount)